import patterns.FactoryMethod.FactoryMethod;
import patterns.SimpleFactory;
import patterns.State.State;
import patterns.TemplateMethod;

//Реализуйте паттерны: Состояние, Шаблонный метод, Простая фабрика, Фабричный метод
public class task_3 {
    public static void main(String[] args) {
        TemplateMethod.main(null);
        State.main(null);
        SimpleFactory.main(null);
        FactoryMethod.main(null);
    }
}
